---
/**
 * VARIANT 6 — STREK. `/lab/skyline` with the photograph removed: the traced
 * line and a flat block of ink are the entire mountain.
 *
 * Same copy, same menu, same type animation, same ridges-as-menu — the only
 * variable is the mountain, which is the only way the comparison means
 * anything. See `Strek.astro` for what removing the film buys and what it
 * costs.
 */
import Layout from '../../layouts/Layout.astro';
import OneScreen from '../../components/OneScreen.astro';
import Strek from '../../components/Strek.astro';
---

<Layout title="Variant — Strek">
  <OneScreen>
    <Strek />
  </OneScreen>
</Layout>
